apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "snappcloud-status-backend-server.fullname" . }}-test-connection"
  labels:
    {{- include "snappcloud-status-backend-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "snappcloud-status-backend-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
