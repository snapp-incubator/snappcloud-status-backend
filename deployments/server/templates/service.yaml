apiVersion: v1
kind: Service
metadata:
  name: {{ include "snappcloud-status-backend-server.fullname" . }}
  labels:
    {{- include "snappcloud-status-backend-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "snappcloud-status-backend-server.selectorLabels" . | nindent 4 }}
