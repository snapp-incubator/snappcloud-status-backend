apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "snappcloud-harbor-client.fullname" . }}-test-connection"
  labels:
    {{- include "snappcloud-harbor-client.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "snappcloud-harbor-client.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
